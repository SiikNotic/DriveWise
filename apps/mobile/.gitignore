# dependencies
node_modules/

# Vite build output
dist/

# debug
npm-debug.*

# macOS
.DS_Store

# local env files
.env*.local

# typescript
*.tsbuildinfo

# android/ has its own .gitignore (auto-generated by `cap add android`)
# covering build/, .gradle/, copied web assets, keystores, etc. — the
# android/ project itself IS committed, unlike the earlier Expo managed
# workflow: this is a real native project we own directly.
